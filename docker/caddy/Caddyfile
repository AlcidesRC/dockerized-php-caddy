{
    admin off

	supervisor {
		php-fpm --nodaemonize {
			env DEBUG false

			redirect_stdout stdout
			redirect_stderr stdout

			restart_policy on_failure
		}
	}
}

import snippets/*

{$CADDY_HOSTNAME} {
    import common
    import ssl
    import domain

    root * /var/www/html/public
}
